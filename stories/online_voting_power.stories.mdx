import {
  Meta, Story, Canvas,
} from '@storybook/addon-docs/blocks';
import { OnlineVotingPower, SelectTabs } from '../src';
import { data } from './utils/online_voting_power';
import { formatNumber } from './utils/convert_number';

<Meta title="OnlineVotingPower" component={OnlineVotingPower} />

# OnlineVotingPower
---
## Default
<Canvas>
  <Story name="Default">
    <OnlineVotingPower
      data={data}
      recharts={{
        cartesianGrid: {
          stroke: '#E8E8E8'
        },
        gridAspect: 3,
        toolTipFormatter: (value) => formatNumber(value)
      }}
    />
  </Story>
</Canvas>

```
<OnlineVotingPower
  data={[
    {
    date: 'JAN 1', value: 1500,
    },
    ...
  ]}
  recharts={{
    cartesianGrid: {
      stroke: '#E8E8E8'
    },
    gridAspect: 3,
    toolTipFormatter: (value) => formatNumber(value)
  }}
/>
```
## Props
<table>
<thead>
  <tr>
    <th>Property</th>
    <th>Required</th>
    <th>Type</th>
    <th>Description</th>
    <th>Default</th>
  </tr>
  </thead>
<tbody>
  <tr>
    <td>linearGradient</td>
    <td>-</td>
    <td>

```
{
  offset: number;
  color: string;
}[]
```

  </td>
    <td>Line graph gradient colors</td>
  <td>

```
[
  {
    offset: 0,
    color: '#FD9526',
  },
  {
    offset: 25,
    color: '#f5d442',
  },
  {
    offset: 50,
    color: '#f03a85',
  },
  {
    offset: 75,
    color: '#b641fa',
  },
]
```

  </td>
  </tr>

  <tr>
    <td>recharts</td>
    <td>-</td>
    <td>

```
{
  cartesianGrid?: {
    stroke?: string;
  },
  axis?: {
    stroke?: string;
    fontSize?: number | string;
  },
  gridAspect?: number;
  toolTipFormatter?: (value:string) => any;
}
```

  </td>
    <td>Settings for the recharts grid</td>
  <td>

```
{
  axis: {
    fontSize: 10,
  },
  gridAspect: 1.5
}
```

  </td>
  </tr>

  <tr>
    <td>data</td>
    <td>true</td>
    <td>

```
{
  date: string;
  value: number;
}[]
```

  </td>
    <td>-</td>
      <td>-</td>
  </tr>

  <tr>
    <td>className</td>
    <td>-</td>
    <td>

`string`

  </td>
    <td>-</td>
    <td>-</td>
  </tr>
  </tbody>
</table>
<br/>

## With SelectTabs
<Canvas>
  <Story name="With SelectTabs">
    <SelectTabs
      tabs={[
        {
          key: 'onlineVotingPower',
          display: 'Online Voting Power',
          component: (
            <OnlineVotingPower
              data={data}
              recharts={{
                cartesianGrid: {
                  stroke: '#E8E8E8'
                },
                gridAspect: 3,
                toolTipFormatter: (value) => formatNumber(value)
              }}
            />
          )
        }
      ]}
    />
  </Story>
</Canvas>

```
<SelectTabs
  tabs={[
    {
      key: 'onlineVotingPower',
      display: 'Online Voting Power',
      component: (
        <OnlineVotingPower
          data={[
            {
            date: 'JAN 1', value: 1500,
            },
            ...
          ]}
          recharts={{
            cartesianGrid: {
              stroke: '#E8E8E8'
            },
            gridAspect: 3,
            toolTipFormatter: (value) => formatNumber(value)
          }}
        />
      )
    }
  ]}
/>
```
