import {
  Meta, Story, Canvas,
} from '@storybook/addon-docs/blocks';
import { ValidatorListDesktop } from '../src';
import { getActiveDummy, labelsList, getInactiveDummy, useTabsHook, useSearchHook } from './utils/validators_list';

<Meta title="ValidatorListDesktop" component={ValidatorListDesktop}/>

# Tabs

---

## Default
<Canvas>
  <Story name="Default">
    <ValidatorListDesktop
      onClick={(data) => console.log(`clicked validator with address: ${data.operatorAddress}`)}
      labels={labelsList}
      active={getActiveDummy('desktop')}
      inactive={getInactiveDummy('desktop')}
      tabs={useTabsHook()}
      search={{
        ...useSearchHook(),
        placeholder: 'A good placeholder',
      }}
    />
  </Story>
</Canvas>

```
<ValidatorListDesktop
  onClick={(data) => console.log(`clicked validator with address: ${data.operatorAddress}`)}
  labels={{
    commission: 'Commission',
    active: 'Active Validators',
    inactive: 'Inactive Validators',
    rank: 'Rank',
    moniker: 'Moniker',
    votingPower: 'Voting Power',
    self: 'Self',
    status: 'Status',
  }}
  active={[
    {
      moniker: {
        rawValue: 'Forbole',
        display: <div>Forbole</div>,
      },
      operatorAddress: '3s12',
      commission: {
        rawValue: 90,
        display: '90%',
      },
      self: {
        rawValue: 11,
        display: '11%',
      },
      votingPower: {
        rawValue: 10,
        display: '10',
        percentDisplay: '45%',
      },
      status: {
        rawValue: 'active',
        className: 'active',
        display: 'active',
      },
    },
    ...
  ]}
  inactive={[
    {
      moniker: {
        rawValue: 'Forbole',
        display: <div>Forbole</div>,
      },
      operatorAddress: '31afsdff2',
      self: {
        rawValue: 10,
        display: '10%',
      },
      votingPower: {
        rawValue: 1000,
        display: '110',
        percentDisplay: '106%',
      },
      status: {
        rawValue: 'active',
        className: 'active',
        display: 'active',
      },
    },
    ...
  ]}
  tabs={{
    value: 1,
    handleChange: () => {}
  }}
  search={{
    value: '',
    handleChange: (e) => console.log(e.target.value),
    placeholder: 'A good placeholder',
  }}
/>
```

## Props
<table>
<thead>
  <tr>
    <th>Property</th>
    <th>Required</th>
    <th>Type</th>
    <th>Description</th>
    <th>Default</th>
  </tr>
</thead>
<tbody>
  <tr>
    <td>labels</td>
    <td>true</td>
    <td>

```
commission: string;
active: string;
inactive: string;
```

  </td>
    <td>-</td>
    <td>-</td>
  </tr>

  <tr>
    <td>active</td>
    <td>true</td>
    <td>

```
{
  operatorAddress: string;
  moniker: {
    display: React.ReactNode;
    rawValue: string;
    imageUrl: string;
  }
  status: {
    rawValue: string;
    className: string;
    display: string;
  }
  commission?: {
    rawValue: number;
    display: string;
  };
  votingPower: {
    rawValue: number;
    display: string;
    percentDisplay: string;
    visual?: React.ReactNode;
  }
}[]
```

  </td>
    <td>-</td>
    <td>-</td>
  </tr>

  <tr>
    <td>inactive</td>
    <td>true</td>
    <td>

```
{
  operatorAddress: string;
  moniker: {
    display: React.ReactNode;
    rawValue: string;
    imageUrl: string;
  }
  status: {
    rawValue: string;
    className: string;
    display: string;
  }
  votingPower: {
    rawValue: number;
    display: string;
    percentDisplay: string;
    visual?: React.ReactNode;
  }
}[]
```

  </td>
    <td>-</td>
    <td>-</td>
  </tr>

  <tr>
    <td>onClick</td>
    <td>-</td>
    <td>

`(data: Data) => void`

  </td>
    <td>-</td>
    <td>-</td>
  </tr>

<tr>
    <td>tabs</td>
    <td>true</td>
    <td>

```
{
  handleChange: (_event: any, newValue: number) => void;
  value: 0 | 1;
}
```

  </td>
    <td>handles mui tabs</td>
    <td>-</td>
  </tr>

  <tr>
    <td>search</td>
    <td>true</td>
    <td>

```
{
  placeholder: string;
  value: string;
  onSearchCallback: (value:string) => void;
  handleChange: (e: React.ChangeEvent<HTMLInputElement>) => void;
}
```

  </td>
    <td>handles search bar</td>
    <td>-</td>
  </tr>

  <tr>
    <td>className</td>
    <td>-</td>
    <td>

`string`

  </td>
    <td>-</td>
    <td>-</td>
  </tr>
  </tbody>
</table>
<br/>
