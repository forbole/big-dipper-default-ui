import {
  Meta, Story, Canvas,
} from '@storybook/addon-docs/blocks';
import {
  ProposalList,
} from '../src';
import {
  dummyData,
} from './utils/proposal_list';

<Meta title="ProposalList" component={ProposalList} />

# ProposalList
---

## Default
<Canvas>
  <Story name="Default">
    <ProposalList
      data={dummyData}
      desktop={false}
      onClick={(data) => console.log(`Row with id ${data.id} clicked`)}
    />
  </Story>
</Canvas>

```
<ProposalList data={[
  {
    id: string | number;
    proposer: React.ReactNode;
    title: React.ReactNode;
    content: string;
    status: {
      current: boolean;
      display: string;
    }
    time: string;
    duration: string;
  },
  ...
  ]}
  desktop={false}
  onClick={(data) => console.log(`Row with id ${data.id} clicked`)}
/>
```

## Props
<table>
<thead>
  <tr>
    <th>Property</th>
    <th>Required</th>
    <th>Type</th>
    <th>Description</th>
    <th>Default</th>
  </tr>
  </thead>
  <tbody>
    <tr>
      <td>data</td>
      <td>true</td>
      <td>

```
{
  id: string;
  proposer: React.ReactNode;
  title: {
    display: string;
    id: string
  }
  content: string;
  status: {
    current: boolean;
    display: string;
  }
  time: string;
  duration?: string;
}[];
```

  </td>
    <td>Data Displayed</td>
    <td>-</td>
  </tr>

  <tr>
    <td>desktop</td>
    <td>-</td>
    <td>

`boolean`

  </td>
    <td>desktop and mobile layouts switching</td>
    <td>-</td>
  </tr>
  <tr>
    <td>className</td>
    <td>-</td>
    <td>

`string`

  </td>
    <td>styles displayed</td>
    <td>-</td>
  </tr>
    <tr>
    <td>onClick</td>
    <td>-</td>
    <td>

`(data) => void`

  </td>
    <td>-</td>
    <td>-</td>
  </tr>
  </tbody>
</table>
<br/>

## Desktop
<Canvas>
  <Story name="Desktop">
    <ProposalList
      data={dummyData}
      desktop={true}
      onClick={(data) => console.log(`Row with id ${data.id} clicked`)}
    />
  </Story>
</Canvas>

```
<ProposalList data={[
  {
    id: string | number;
    proposer: React.ReactNode;
    title: React.ReactNode;
    content: string;
    status: {
      current: boolean;
      display: string;
    }
    time: string;
    duration: string;
  },
  ...
  ]}
  desktop={true}
  onClick={(data) => console.log(`Row with id ${data.id} clicked`)}
/>
```
