import {
  Meta, Story, Canvas,
} from '@storybook/addon-docs/blocks';
import { ValidatorListDesktop } from '../src';
import { dummyData, labelsList, dummyDataInactive, useTabsHook, useSearchHook } from './utils/validators_list';

<Meta title="ValidatorListDesktop" component={ValidatorListDesktop}/>

# Tabs

---

## Default
<Canvas>
  <Story name="Default">
    <ValidatorListDesktop
      onClick={(data) => console.log(`clicked validator with address: ${data.operatorAddress}`)}
      labels={labelsList}
      active={dummyData}
      inactive={dummyDataInactive}
      tabs={useTabsHook()}
      search={{
        ...useSearchHook(),
        placeholder: 'A good placeholder',
      }}
    />
  </Story>
</Canvas>

```
<ValidatorListMobile
  onClick={(data) => console.log(`clicked validator with address: ${data.operatorAddress}`)}
  labels={{
    commission: 'Commission',
    active: 'Active Validators',
    inactive: 'Inactive Validators',
  }}
  active={[
    {
      imageUrl: 'https://s3.amazonaws.com/keybase_processed_uploads/f5b0771af36b2e3d6a196a29751e1f05_360_360.jpeg',
      displayName: 'Forbsdsfgkbhfskhfsdhole',
      operatorAddress: '312',
      commission: '100%',
      votingPower: {
        rawValue: 1000,
        display: '110,000,08511111111321654649845',
        percentDisplay: '10654654654654654654654564%',
      },
      status: {
        className: 'active',
        display: 'active',
      },
    },
    ...
  ]}
  inactive={[
    {
      imageUrl: 'https://s3.amazonaws.com/keybase_processed_uploads/f5b0771af36b2e3d6a196a29751e1f05_360_360.jpeg',
      displayName: 'Forbsdsfgkbhfskhfsdhole',
      operatorAddress: '312',
      votingPower: {
        rawValue: 1000,
        display: '110,000,08511111111321654649845',
        percentDisplay: '10654654654654654654654564%',
      },
      status: {
        className: 'active',
        display: 'active',
      },
    },
    ...
  ]}
/>
```
## Props
<table>
<thead>
  <tr>
    <th>Property</th>
    <th>Required</th>
    <th>Type</th>
    <th>Description</th>
    <th>Default</th>
  </tr>
</thead>
<tbody>
  <tr>
    <td>labels</td>
    <td>true</td>
    <td>

```
commission: string;
active: string;
inactive: string;
```

  </td>
    <td>-</td>
    <td>-</td>
  </tr>

  <tr>
    <td>active</td>
    <td>true</td>
    <td>

```
{
  imageUrl: string;
  displayName: string;
  operatorAddress: string;
  status: {
    className: string;
    display: string;
  }
  commission?: string;
  votingPower: {
    rawValue: number;
    display: string;
    percentDisplay: string;
  }
}[]
```

  </td>
    <td>-</td>
    <td>-</td>
  </tr>

  <tr>
    <td>inactive</td>
    <td>true</td>
    <td>

```
{
  imageUrl: string;
  displayName: string;
  operatorAddress: string;
  status: {
    className: string;
    display: string;
  }
  commission?: string;
  votingPower: {
    rawValue: number;
    display: string;
    percentDisplay: string;
  }
}[]
```

  </td>
    <td>-</td>
    <td>-</td>
  </tr>

  <tr>
    <td>onClick</td>
    <td>-</td>
    <td>

`(data: Data) => void`

  </td>
    <td>-</td>
    <td>-</td>
  </tr>

  <tr>
    <td>className</td>
    <td>-</td>
    <td>

`string`

  </td>
    <td>-</td>
    <td>-</td>
  </tr>
  </tbody>
</table>
<br/>
